// Single source of truth for the app's release identity. Bump APP_VERSION on
// each release and tag the matching commit in git (e.g. `git tag v1.0.0`).
// Consumed by the UI version watermark -- see components/VersionFooter.js.
export const APP_NAME = 'OBGYN Master Rotation';
export const APP_VERSION = '1.0.0';
export const APP_AUTHOR = 'Ruel Palado';
export const APP_YEAR = 2026;
